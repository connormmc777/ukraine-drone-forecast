apiVersion: v1
kind: Service
metadata:
  name: {{ include "dronespredictions.fullname" . }}
  labels:
    {{- include "dronespredictions.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "dronespredictions.selectorLabels" . | nindent 4 }}
